/**
 * Created by AJassra on 1/28/14.
 *//*

describe("TodoApp.controllers", function() {
    var ctrl, scope, todoSrvc;

    beforeEach(module('TodoApp'));

    beforeEach(inject(function ($rootScope, $controller) {
        scope = $rootScope.$new();
        ctrl = $controller('DashboardController',
            {
                $scope: scope,
                TodoSrvc: {
                    get: function() {
                        return [{
                            title: 'F#',
                            desc: 'learn f#'
                        }, {
                            title: 'lib',
                            desc: 'return books'
                        }];
                    }
                }
            });
    }));

    it('has initialized "searchBy" property is defined and set as null', function() {
        expect(scope.searchBy).toBeDefined();
        //expect(scope.searchBy).toBeNull();
    });

     */
/*it('has initialized "isDisabled" property', function() {
        expect(scope.isDisabled).toBeTruthy();
    });

    it('has initialized "notes" property is defined, not null and expect count is 2', function() {
        expect(scope.notes).toBeDefined();
        expect(scope.notes).not.toBeNull();
        expect(scope.notes.length).toBe(2);
    });

    it('The length of "onSearch" function is present', function() {
        expect(scope.onSearch).toBeDefined();
    });*//*


});
*/
